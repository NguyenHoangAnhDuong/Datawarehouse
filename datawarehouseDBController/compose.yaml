services:
  postgres:
    image: 'postgres:14-alpine3.20'
    environment:
      - 'POSTGRES_DB=DBController'
      - 'POSTGRES_PASSWORD=DBController'
      - 'POSTGRES_USER=user'
    ports:
      - '5454:5432'
