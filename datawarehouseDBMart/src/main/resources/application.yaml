server:
  port: 8088
  servlet:
    context-path: /DBMart

spring:
  datasource:
    url: jdbc:postgresql://localhost:5456/DBMart
    driverClassName: org.postgresql.Driver
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true